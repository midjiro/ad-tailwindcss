export type TwOptions = {
    minWidth: number;
    maxWidth: number;
    baseFontSize: number;
};
export type Sizes = {
    min: number;
    max: number;
    slope: number;
    intercept: number;
};
export type Property = Record<string, string | string[]>;
